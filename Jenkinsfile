node{
    def a="im into test stage"
    def b="im into build stage"
    def c="im into deploy stage"

    stage('test'){
        echo "${a}"
    }

    stage('build'){
        echo "${b}"
    }

    stage('deploy'){
        echo "${c}"
    }
}